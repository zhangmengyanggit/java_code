<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.web.mapper.SysRegionMapper">

    <resultMap type="SysRegion" id="SysRegionResult">
        <result property="regionId"    column="region_id"    />
        <result property="regionName"    column="region_name"    />
        <result property="regionShortName"    column="region_short_name"    />
        <result property="regionCode"    column="region_code"    />
        <result property="regionParentId"    column="region_parent_id"    />
        <result property="regionLevel"    column="region_level"    />
    </resultMap>

    <sql id="selectSysRegionVo">
        select region_id, region_name, region_short_name, region_code, region_parent_id, region_level from sys_region
    </sql>

    <select id="selectSysRegionList" parameterType="SysRegion" resultMap="SysRegionResult">
        <include refid="selectSysRegionVo"/>
        <where>
            <if test="regionName != null  and regionName != ''"> and region_name like concat('%', #{regionName}, '%')</if>
            <if test="regionShortName != null  and regionShortName != ''"> and region_short_name like concat('%', #{regionShortName}, '%')</if>
            <if test="regionCode != null  and regionCode != ''"> and region_code = #{regionCode}</if>
            <if test="regionParentId != null  and regionParentId != ''"> and region_parent_id = #{regionParentId}</if>
            <if test="regionLevel != null "> and region_level = #{regionLevel}</if>
        </where>
    </select>

    <select id="selectSysRegionByRegionId" parameterType="String" resultMap="SysRegionResult">
        <include refid="selectSysRegionVo"/>
        where region_id = #{regionId}
    </select>


</mapper>